org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# The repository is developed under a Chinese Windows path; AGP otherwise
# rejects the project before compilation even though the path is valid.
android.overridePathCheck=true
# Pub packages are cached on C: while this worktree is on D:. Kotlin's
# incremental cache cannot relativize those paths across drive roots.
kotlin.incremental=false
