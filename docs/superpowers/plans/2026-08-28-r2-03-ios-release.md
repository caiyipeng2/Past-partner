# R2-03 iOS Release Preparation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax to track progress.

**Goal:** Make iOS release verification reproducible on macOS while keeping Windows evidence limited to source checks and avoiding false claims about signing or devices.

**Architecture:** A small Python policy checker validates the Flutter version against `ios/Runner/Info.plist` and rejects ATS exceptions without requiring Xcode. A manually-triggered macOS GitHub Actions job runs the checker, Flutter analysis/tests, and an unsigned IPA build; Apple signing/export credentials remain a separate explicitly gated task.

**Tech Stack:** Python 3 standard library, `plistlib`, Flutter/Dart, GitHub Actions, macOS runner.

---

### Task 1: Add iOS source policy checker and macOS no-codesign workflow

**Files:**
- Create: `scripts/check_ios_release.py`
- Create: `.github/workflows/ios-release.yml`
- Create: `tests/integration/test_ios_release_contract.py`

- [x] **Step 1: Write failing policy/workflow tests**
- [x] **Step 2: Run the tests and confirm missing checker/workflow failures**
- [x] **Step 3: Implement the checker and manual macOS workflow**
- [x] **Step 4: Run focused tests and static verification**
- [x] **Step 5: Commit the source-policy slice and wait for user acceptance**

### Task 2: Add explicit Apple signing/export gate

**Files:**
- Modify: `.github/workflows/ios-release.yml`
- Create: `scripts/validate_ios_signing.sh`
- Create: `tests/integration/test_ios_signing_contract.py`

- [x] **Step 1: Write failing signing-gate contract tests**
- [x] **Step 2: Run the tests and confirm the signing script/job is missing**
- [x] **Step 3: Implement the macOS-only signing gate and secret-backed workflow job**
- [x] **Step 4: Run Windows static tests and shell-source checks**
- [x] **Step 5: Commit the signing/export gate and wait for user acceptance**

Implement certificate/profile/keychain inputs only through GitHub Actions secrets, fail closed when any required input is missing, and keep signing material outside the repository. This task must not run on Windows and must not echo certificate, profile, keychain, or App Store Connect values.

### Task 3: macOS archive, device, and store verification handoff

Run the manual workflow on a macOS runner, attach only non-secret build metadata, and record archive/signing/device evidence. Windows may only report source-policy and workflow-contract results; it cannot mark Xcode archive or iOS device verification complete.
